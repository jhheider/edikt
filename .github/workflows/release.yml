name: Release

# Publish a release with `gh release create vX.Y.Z --generate-notes` (versions
# already committed). The workflow_dispatch path rebuilds an existing release's
# binaries and refreshes the tap formula, without re-publishing crates.
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to rebuild binaries + refresh formula (e.g. v0.2.1)"
        required: true

jobs:
  release:
    uses: jhheider/rust-ci/.github/workflows/release.yml@v1
    with:
      bin-name: edikt
      tag: ${{ github.event.inputs.tag }}
      gen-docs: true
      publish-crates: true
      crates: "edikt-core edikt-syntax edikt-jsonc edikt-ini edikt-env edikt-toml edikt-yaml edikt-kdl edikt"
      tap-repo: jhheider/homebrew-tap
      tap-formula: Formula/edikt.rb
      targets: |
        [
          {"os":"ubuntu-latest","target":"x86_64-unknown-linux-gnu","asset":"edikt-linux-x86_64.tar.gz"},
          {"os":"ubuntu-latest","target":"aarch64-unknown-linux-gnu","asset":"edikt-linux-aarch64.tar.gz"},
          {"os":"macos-latest","target":"x86_64-apple-darwin","asset":"edikt-macos-x86_64.tar.gz"},
          {"os":"macos-latest","target":"aarch64-apple-darwin","asset":"edikt-macos-aarch64.tar.gz"},
          {"os":"windows-latest","target":"x86_64-pc-windows-msvc","asset":"edikt-windows-x86_64.zip"}
        ]
    secrets: inherit
