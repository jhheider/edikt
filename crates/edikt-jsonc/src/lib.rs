//! edikt JSONC/JSON5/JSON format module.
//!
//! A lossless `rowan` + `logos` CST (validated by the day-zero spike) with typed
//! accessors, `FEATURES`, and impls of `Document` + `Convert`. Populated by the
//! JSONC slice.
