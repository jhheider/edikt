//! edikt .env / .properties format module.
//!
//! Flat, string-valued, honest line-level editing only — no grammar, no
//! interpolation, no coercion in storage. `FEATURES` + `Document` + `Convert`.
//! Populated by the env slice.
