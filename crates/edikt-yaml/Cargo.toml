[package]
name = "edikt-yaml"
version = "0.3.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
description = "edikt YAML format module: lossless in-place edit, query, and conversion (pure Rust, via libyaml-safer)."
readme = { workspace = true }

[dependencies]
edikt-core = { workspace = true }
# Pure-Rust libyaml port (safe API, zero transitive deps). We drive its event
# stream for both the data model (query/convert) and byte-precise marks (the
# lossless splice) - one parser, three jobs.
libyaml-safer = "0.3"
thiserror = { workspace = true }
